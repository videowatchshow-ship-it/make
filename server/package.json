{
  "name": "centbeam-server",
  "version": "0.1.0",
  "private": true,
  "description": "CENTBEAM relay API (destinations CRUD + health). Media fan-out via MediaMTX + fanout.sh.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "audit": "npm audit --omit=dev --audit-level=high",
    "audit:fix": "npm audit fix"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}

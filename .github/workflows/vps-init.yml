name: vps-init

# VPS initial setup completed 2026-09-03
# This workflow is now disabled (setup done)
# To re-run, use vps-setup.yml with workflow_dispatch

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "VPS initial setup was completed on 2026-09-03"
          echo "nginx, certbot, site directories all configured"
          echo "Use vps-setup.yml for future VPS operations"

name: Diagnose gauth excel encoding + sort
on:
  workflow_dispatch:
  push:
    branches: ['claude/gauth-frontend-backend-fixes-cg2icv']
    paths: ['.github/trigger-diag-gauth.txt']

jobs:
  diag:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Verify label column raw
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.GAUTH_HOST }}
          username: ${{ secrets.GAUTH_USER }}
          key: ${{ secrets.GAUTH_SSH_KEY }}
          command_timeout: 60s
          script: |
            sudo node -e "
            const XLSX = require('/opt/gauth-full/node_modules/xlsx');
            const fs = require('fs');
            const dir = '/opt/gauth-full/uploads/archive';
            const files = fs.readdirSync(dir).filter(f=>f.toLowerCase().endsWith('.xlsx'));
            const fp = dir + '/' + files[0];
            const wb = XLSX.readFile(fp);
            const sh = wb.Sheets[wb.SheetNames[0]];
            const rows = XLSX.utils.sheet_to_json(sh, {header:1, raw:true, defval:''});
            const r15 = rows[14], r16 = rows[15];
            console.log('Row15 col7 (label):', JSON.stringify(r15[7]));
            console.log('Row15 col7 bytes (hex):', Buffer.from(String(r15[7]),'utf8').toString('hex'));
            console.log('Row16 col7 (label):', JSON.stringify(r16[7]));
            console.log('Row16 col7 bytes (hex):', Buffer.from(String(r16[7]),'utf8').toString('hex'));
            " 2>&1
            echo "=== DONE ==="
